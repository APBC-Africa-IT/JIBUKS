{
  "name": "jibuks-backend",
  "private": true,
  "version": "0.1.0",
  "description": "JiBUks backend - Node.js/TypeScript modular monolith and shared domain packages.",
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --build --force"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4",
    "@types/node": "^22.9.0"
  },
  "engines": {
    "node": ">=20"
  }
}
